<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="style.css">
	<title>Tempus Year Progress</title>
</head>

<body>
  <main>
    <header class="progress-header">
      <h2>
        {{ year }}
      </h2>
      <p class="subtitle">Progress</p>
      <p class="date">as of {{ the_date }}</p>
    </header>

    <section id="progress">
      <div class="graph">
        <div class="bar" style="width: {{ pct | round(4) }}%">
          &nbsp;
        </div>
        <div class="gridlines">
          {% for i in range(13) %}
						{% if i >= (pct * 12/100 - 0.0002)|round(0, "ceil") %}
							<div class="line hidden"></div>
						{% else %}
							<div class="line"></div>
						{% endif %}
					{% endfor %}
        </div>
      </div>

      <div class="labels">
        <span>
          <strong>{{ pct | round(0) }}</strong>% done
        </span>
        <span>
          <strong>{{ current_day }}</strong> / {{ total_days }} days
        </span>
      </div>
    </section>
  </main>
</body>

</html>
